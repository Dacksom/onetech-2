services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    # Puerto NO expuesto para evitar conflicto, Dokploy + Traefik lo rutean internamente
    networks:
      - dokploy-network
    restart: unless-stopped

networks:
  dokploy-network:
    external: true
